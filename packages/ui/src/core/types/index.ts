/**
 * Core Types Module
 *
 * Re-exports all type definitions from the core layer.
 * This provides a single entry point for importing types.
 *
 * @module core/types
 */

export type {
  Alignment,
  AnimationTiming,
  AsyncCallback,
  Callback,
  ClassName,
  ColorIntent,
  InteractiveState,
  Justify,
  OptionalProps,
  Orientation,
  Position,
  Radius,
  RequiredProps,
  Size,
  Spacing,
  ValueOf,
  ZLayer,
} from "./common.types";
