/**
 * Shared Types Module
 *
 * Re-exports types that are commonly used across presentation components.
 * These are convenience re-exports from the core types module.
 *
 * @module shared/types
 */

// Re-export commonly used types from core
export type {
  Alignment,
  Callback,
  ClassName,
  ColorIntent,
  Orientation,
  Position,
  Size,
} from "../../core/types";
